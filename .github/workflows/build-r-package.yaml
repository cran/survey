name: Build survey

on:
  workflow_dispatch:
  push:

jobs:
  build:
    name: Build survey
    permissions:
      checks: write
      contents: read
    uses: Displayr/nixr-public/.github/workflows/build-flake-installable.yaml@master
    with:
      installable-path: pkgs.x86_64-linux.rPackages.survey
      build-args: --override-input displayr-survey .
      installable-name: survey
    secrets: inherit